# --- AI ENGINE ---
# Основной модуль для работы с нейросетевыми стратегиями
# ВАЖНО: Используется версия 0.28.1 для поддержки ChatCompletion.acreate
openai==0.28.1

# --- CORE ASYNC & NETWORK ---
# Обеспечивают асинхронность и работу веб-терминала на порту 3000
asyncio
aiohttp
aiohttp_cors
python-dotenv

# --- BLOCKCHAIN (TON PROTOCOL) ---
# Библиотека для связи с сетью TON (исправляет ошибку [FATAL])
pytoniq

# --- MATH & NEURAL ANALYTICS ---
# Для спектрального анализа (FFT) и фрактальных вычислений
numpy

# --- INFRASTRUCTURE : DATABASE ---
# Драйверы для связи с PostgreSQL на node1.pghost.ru:32865
asyncpg
psycopg2-binary
