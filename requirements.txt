# --- AI ENGINE ---
# Основной модуль для работы с нейросетевыми стратегиями
openai>=1.0.0

# --- CORE ASYNC & NETWORK ---
# Обеспечивают асинхронность и работу веб-терминала на порту 3000
asyncio
aiohttp
aiohttp_cors
python-dotenv

# --- BLOCKCHAIN (TON PROTOCOL) ---
# Критически важная библиотека для связи с сетью TON (исправляет ошибку [FATAL])
pytoniq

# --- MATH & NEURAL ANALYTICS ---
# Используется для спектрального анализа (FFT) и фрактальных вычислений
numpy

# --- INFRASTRUCTURE : DATABASE ---
# Драйверы для надежной связи с PostgreSQL на node1.pghost.ru:32865
asyncpg
psycopg2-binary
